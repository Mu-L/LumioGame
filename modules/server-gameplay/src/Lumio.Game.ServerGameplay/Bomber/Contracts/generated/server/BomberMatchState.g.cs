// <auto-generated/>
#nullable enable
using System;
using Lumio.GameRuntime.Ecs;

namespace Lumio.Game.ServerGameplay.Bomber.Contracts.Components;

public sealed partial class BomberMatchState : IGeneratedComponent, IGeneratedSyncMetadata
{
    partial void OnMatchTickChanging(ulong old, ulong @new, ChangeReason reason);
    partial void OnMatchTickChanged(ulong old, ulong @new, ChangeReason reason);
    partial void OnStartTickChanging(ulong old, ulong @new, ChangeReason reason);
    partial void OnStartTickChanged(ulong old, ulong @new, ChangeReason reason);
    partial void OnEndTickChanging(ulong old, ulong @new, ChangeReason reason);
    partial void OnEndTickChanged(ulong old, ulong @new, ChangeReason reason);
    partial void OnPhaseChanging(int old, int @new, ChangeReason reason);
    partial void OnPhaseChanged(int old, int @new, ChangeReason reason);
    partial void OnHatKingNetEntityIdRawChanging(ulong old, ulong @new, ChangeReason reason);
    partial void OnHatKingNetEntityIdRawChanged(ulong old, ulong @new, ChangeReason reason);
    partial void OnClientWrite(in SyncWrite w, ref bool accept);


    void IGeneratedComponent.BindFields(ISyncHost host)
    {
        MatchTick = MatchTick.Bound(host, this, 0, "BomberMatchState.matchTick");
        StartTick = StartTick.Bound(host, this, 1, "BomberMatchState.startTick");
        EndTick = EndTick.Bound(host, this, 2, "BomberMatchState.endTick");
        Phase = Phase.Bound(host, this, 3, "BomberMatchState.phase");
        HatKingNetEntityIdRaw = HatKingNetEntityIdRaw.Bound(host, this, 4, "BomberMatchState.hatKingNetEntityIdRaw");
    }

    void IGeneratedComponent.InvokePostAttribute() => PostAttribute();
    partial void PostAttribute();

    void IGeneratedComponent.InvokeFieldChanging(int ordinal, object? oldValue, object? newValue, ChangeReason reason)
    {
        if (ordinal == 0) OnMatchTickChanging((ulong)oldValue!, (ulong)newValue!, reason);
        if (ordinal == 1) OnStartTickChanging((ulong)oldValue!, (ulong)newValue!, reason);
        if (ordinal == 2) OnEndTickChanging((ulong)oldValue!, (ulong)newValue!, reason);
        if (ordinal == 3) OnPhaseChanging((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 4) OnHatKingNetEntityIdRawChanging((ulong)oldValue!, (ulong)newValue!, reason);
    }

    void IGeneratedComponent.InvokeFieldChanged(int ordinal, object? oldValue, object? newValue, ChangeReason reason)
    {
        if (ordinal == 0) OnMatchTickChanged((ulong)oldValue!, (ulong)newValue!, reason);
        if (ordinal == 1) OnStartTickChanged((ulong)oldValue!, (ulong)newValue!, reason);
        if (ordinal == 2) OnEndTickChanged((ulong)oldValue!, (ulong)newValue!, reason);
        if (ordinal == 3) OnPhaseChanged((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 4) OnHatKingNetEntityIdRawChanged((ulong)oldValue!, (ulong)newValue!, reason);
    }

    bool IGeneratedComponent.DispatchClientWrite(in SyncWrite write)
    {
        bool accept = true;
        OnClientWrite(in write, ref accept);
        return accept;
    }

    void IGeneratedComponent.DispatchServerRpc(string method, object?[] args)
    {
    }

    void IGeneratedComponent.DispatchClientRpc(string method, object?[] args)
    {
    }

    void IGeneratedComponent.CapturePersist(IPersistWriter writer)
    {
        writer.WriteUInt64("BomberMatchState.matchTick", MatchTick.Value);
        writer.WriteUInt64("BomberMatchState.startTick", StartTick.Value);
        writer.WriteUInt64("BomberMatchState.endTick", EndTick.Value);
        writer.WriteUInt64("BomberMatchState.hatKingNetEntityIdRaw", HatKingNetEntityIdRaw.Value);
    }

    void IGeneratedComponent.CaptureSync(IPersistWriter writer)
    {
        writer.WriteUInt64("BomberMatchState.matchTick", MatchTick.Value);
        writer.WriteUInt64("BomberMatchState.startTick", StartTick.Value);
        writer.WriteUInt64("BomberMatchState.endTick", EndTick.Value);
        writer.WriteUInt64("BomberMatchState.hatKingNetEntityIdRaw", HatKingNetEntityIdRaw.Value);
    }

    void IGeneratedComponent.RestorePersist(IPersistReader reader)
    {
        if (reader.TryReadUInt64("BomberMatchState.matchTick", out ulong matchTickRestore))
            MatchTick.SetSilent(matchTickRestore);
        if (reader.TryReadUInt64("BomberMatchState.startTick", out ulong startTickRestore))
            StartTick.SetSilent(startTickRestore);
        if (reader.TryReadUInt64("BomberMatchState.endTick", out ulong endTickRestore))
            EndTick.SetSilent(endTickRestore);
        if (reader.TryReadUInt64("BomberMatchState.hatKingNetEntityIdRaw", out ulong hatKingNetEntityIdRawRestore))
            HatKingNetEntityIdRaw.SetSilent(hatKingNetEntityIdRawRestore);
    }

    object? IGeneratedComponent.ReadField(string fieldId)
    {
        if (string.Equals(fieldId, "matchTick", StringComparison.Ordinal)) return MatchTick.Value;
        if (string.Equals(fieldId, "startTick", StringComparison.Ordinal)) return StartTick.Value;
        if (string.Equals(fieldId, "endTick", StringComparison.Ordinal)) return EndTick.Value;
        if (string.Equals(fieldId, "phase", StringComparison.Ordinal)) return Phase.Value;
        if (string.Equals(fieldId, "hatKingNetEntityIdRaw", StringComparison.Ordinal)) return HatKingNetEntityIdRaw.Value;
        return null;
    }

    void IGeneratedComponent.WriteField(string fieldId, object? value, bool silent)
    {
        if (string.Equals(fieldId, "matchTick", StringComparison.Ordinal))
        {
            if (silent) MatchTick.SetSilent((ulong)value!);
            else MatchTick.Value = (ulong)value!;
            return;
        }
        if (string.Equals(fieldId, "startTick", StringComparison.Ordinal))
        {
            if (silent) StartTick.SetSilent((ulong)value!);
            else StartTick.Value = (ulong)value!;
            return;
        }
        if (string.Equals(fieldId, "endTick", StringComparison.Ordinal))
        {
            if (silent) EndTick.SetSilent((ulong)value!);
            else EndTick.Value = (ulong)value!;
            return;
        }
        if (string.Equals(fieldId, "phase", StringComparison.Ordinal))
        {
            if (silent) Phase.SetSilent((int)value!);
            else Phase.Value = (int)value!;
            return;
        }
        if (string.Equals(fieldId, "hatKingNetEntityIdRaw", StringComparison.Ordinal))
        {
            if (silent) HatKingNetEntityIdRaw.SetSilent((ulong)value!);
            else HatKingNetEntityIdRaw.Value = (ulong)value!;
            return;
        }
    }

    bool IGeneratedSyncMetadata.TryGetSyncField(string fieldId, out ISyncField field)
    {
        if (string.Equals(fieldId, "matchTick", StringComparison.Ordinal)) { field = MatchTick; return true; }
        if (string.Equals(fieldId, "startTick", StringComparison.Ordinal)) { field = StartTick; return true; }
        if (string.Equals(fieldId, "endTick", StringComparison.Ordinal)) { field = EndTick; return true; }
        if (string.Equals(fieldId, "phase", StringComparison.Ordinal)) { field = Phase; return true; }
        if (string.Equals(fieldId, "hatKingNetEntityIdRaw", StringComparison.Ordinal)) { field = HatKingNetEntityIdRaw; return true; }
        field = null!;
        return false;
    }
}
