// <auto-generated/>
#nullable enable
using System;
using Lumio.GameRuntime.Ecs;

namespace Lumio.Game.ServerGameplay.Bomber.Contracts.Components;

public sealed partial class BomberPlayerState : IGeneratedComponent, IGeneratedSyncMetadata
{
    partial void OnHatCountChanging(int old, int @new, ChangeReason reason);
    partial void OnHatCountChanged(int old, int @new, ChangeReason reason);
    partial void OnRespawnAtTickChanging(ulong old, ulong @new, ChangeReason reason);
    partial void OnRespawnAtTickChanged(ulong old, ulong @new, ChangeReason reason);
    partial void OnProtectedUntilTickChanging(ulong old, ulong @new, ChangeReason reason);
    partial void OnProtectedUntilTickChanged(ulong old, ulong @new, ChangeReason reason);
    partial void OnClientWrite(in SyncWrite w, ref bool accept);


    void IGeneratedComponent.BindFields(ISyncHost host)
    {
        HatCount = HatCount.Bound(host, this, 0, "BomberPlayerState.hatCount");
        RespawnAtTick = RespawnAtTick.Bound(host, this, 1, "BomberPlayerState.respawnAtTick");
        ProtectedUntilTick = ProtectedUntilTick.Bound(host, this, 2, "BomberPlayerState.protectedUntilTick");
    }

    void IGeneratedComponent.InvokePostAttribute() => PostAttribute();
    partial void PostAttribute();

    void IGeneratedComponent.InvokeFieldChanging(int ordinal, object? oldValue, object? newValue, ChangeReason reason)
    {
        if (ordinal == 0) OnHatCountChanging((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 1) OnRespawnAtTickChanging((ulong)oldValue!, (ulong)newValue!, reason);
        if (ordinal == 2) OnProtectedUntilTickChanging((ulong)oldValue!, (ulong)newValue!, reason);
    }

    void IGeneratedComponent.InvokeFieldChanged(int ordinal, object? oldValue, object? newValue, ChangeReason reason)
    {
        if (ordinal == 0) OnHatCountChanged((int)oldValue!, (int)newValue!, reason);
        if (ordinal == 1) OnRespawnAtTickChanged((ulong)oldValue!, (ulong)newValue!, reason);
        if (ordinal == 2) OnProtectedUntilTickChanged((ulong)oldValue!, (ulong)newValue!, reason);
    }

    bool IGeneratedComponent.DispatchClientWrite(in SyncWrite write)
    {
        bool accept = true;
        OnClientWrite(in write, ref accept);
        return accept;
    }

    void IGeneratedComponent.DispatchServerRpc(string method, object?[] args)
    {
    }

    void IGeneratedComponent.DispatchClientRpc(string method, object?[] args)
    {
    }

    void IGeneratedComponent.CapturePersist(IPersistWriter writer)
    {
        writer.WriteUInt64("BomberPlayerState.respawnAtTick", RespawnAtTick.Value);
        writer.WriteUInt64("BomberPlayerState.protectedUntilTick", ProtectedUntilTick.Value);
    }

    void IGeneratedComponent.CaptureSync(IPersistWriter writer)
    {
        writer.WriteUInt64("BomberPlayerState.respawnAtTick", RespawnAtTick.Value);
        writer.WriteUInt64("BomberPlayerState.protectedUntilTick", ProtectedUntilTick.Value);
    }

    void IGeneratedComponent.RestorePersist(IPersistReader reader)
    {
        if (reader.TryReadUInt64("BomberPlayerState.respawnAtTick", out ulong respawnAtTickRestore))
            RespawnAtTick.SetSilent(respawnAtTickRestore);
        if (reader.TryReadUInt64("BomberPlayerState.protectedUntilTick", out ulong protectedUntilTickRestore))
            ProtectedUntilTick.SetSilent(protectedUntilTickRestore);
    }

    object? IGeneratedComponent.ReadField(string fieldId)
    {
        if (string.Equals(fieldId, "hatCount", StringComparison.Ordinal)) return HatCount.Value;
        if (string.Equals(fieldId, "respawnAtTick", StringComparison.Ordinal)) return RespawnAtTick.Value;
        if (string.Equals(fieldId, "protectedUntilTick", StringComparison.Ordinal)) return ProtectedUntilTick.Value;
        return null;
    }

    void IGeneratedComponent.WriteField(string fieldId, object? value, bool silent)
    {
        if (string.Equals(fieldId, "hatCount", StringComparison.Ordinal))
        {
            if (silent) HatCount.SetSilent((int)value!);
            else HatCount.Value = (int)value!;
            return;
        }
        if (string.Equals(fieldId, "respawnAtTick", StringComparison.Ordinal))
        {
            if (silent) RespawnAtTick.SetSilent((ulong)value!);
            else RespawnAtTick.Value = (ulong)value!;
            return;
        }
        if (string.Equals(fieldId, "protectedUntilTick", StringComparison.Ordinal))
        {
            if (silent) ProtectedUntilTick.SetSilent((ulong)value!);
            else ProtectedUntilTick.Value = (ulong)value!;
            return;
        }
    }

    bool IGeneratedSyncMetadata.TryGetSyncField(string fieldId, out ISyncField field)
    {
        if (string.Equals(fieldId, "hatCount", StringComparison.Ordinal)) { field = HatCount; return true; }
        if (string.Equals(fieldId, "respawnAtTick", StringComparison.Ordinal)) { field = RespawnAtTick; return true; }
        if (string.Equals(fieldId, "protectedUntilTick", StringComparison.Ordinal)) { field = ProtectedUntilTick; return true; }
        field = null!;
        return false;
    }
}
